The general rule is:

RouteConstants → Routing and navigation.
ApiConstants → Service layer.
MessageConstants → Toasts, alerts, validation messages.
AppConstants → Configuration, enums, storage keys, limits.


/**
 * TODO:
 *
 * 1. Replace alerts with Toast notifications.
 * 2. Add react-hook-form.
 * 3. Add Yup/Zod validation.
 * 4. Add image size validation.
 * 5. Add image type validation.
 * 6. Add API error handling utility.
 * 7. Add loading spinner component.
 * 8. Add success redirect to Food List page.
 */



src
│
├── assets
│   └── images
│       └── food-placeholder.png
│
├── components
│   ├── Common
│   │     ├── LoadingSpinner.jsx
│   │     ├── ErrorAlert.jsx
│   │     └── EmptyState.jsx
│   │
│   └── Food
│         └── FoodTable.jsx
│
├── pages
│      └── FoodList
│             └── FoodList.jsx
│
├── services
│      ├── ApiClient.js
│      └── FoodService.js
│
└── utils
       └── ImageUtils.js


































